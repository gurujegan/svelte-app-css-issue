/** @type {import('./$types').PageLoad} */
export function load() {

  const contactInfo = {
    name: 'Gurujegan',
    address: 'lakshmi nagar, Nanganallur',
    location: 'Chennai'
  }

  return contactInfo;
}